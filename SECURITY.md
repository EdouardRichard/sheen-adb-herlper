# 安全政策

## 支持范围

安全修复优先覆盖最新发布版本与 `main` 分支。v0.01 不提供后端服务；主要安全边界是本机 ADB 主机身份、配对码、设备地址、Shell 与 Logcat 内容。

## 私密报告

请通过仓库维护者公布的私密安全报告渠道提交问题，不要在公开 Issue 中附加 ADB 私钥、证书、配对码、真实 IP、序列号、Shell 输出、Logcat、签名文件或可识别设备的信息。报告应使用脱敏复现步骤和应用显示的结构化技术代码。

维护者会先确认影响、止损与轮换受影响身份，再修复、回归和发布。请在修复发布前避免公开可利用细节。
